services: 
  web: 
    build:
      context: app
      dockerfile: Dockerfile
    env_file: 
      - .env
    environment:
      - APP_PORT=${APP_PORT}
    ports: 
      - '${APP_PORT}:${APP_PORT}'
    network_mode: "host"